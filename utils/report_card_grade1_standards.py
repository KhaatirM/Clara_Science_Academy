"""1st grade Language Arts and Math standards checklist content (director report card pages 2–3)."""

QUARTER_COLUMNS = ['Q1', 'Q2', 'Q3', 'Q4']

GRADE1_LANGUAGE_ARTS = {
    'title': 'Language Arts',
    'sections': [
        {
            'title': 'LEARNING TO READ INDEPENDENTLY',
            'standards': [
                {
                    'id': 'la_lri_1',
                    'text': (
                        'Demonstrate concept of print, how print is organized and used in reading and writing '
                        'tasks -Identify the parts of a book, use correct book handling skills, display '
                        'directionality, identify various forms of print, Understanding the relationship between '
                        'print and the spoken word, and differentiate between a word and a letter.'
                    ),
                },
                {
                    'id': 'la_lri_2',
                    'text': (
                        'Demonstrate phonemic awareness, the ability to hear and manipulate sounds in spoken words-'
                        'Distinguish initial, medial and final sounds in single-syllable words, distinguish long- '
                        'and short-vowel sounds in orally stated single-syllable words, orally blend three or more '
                        'spoken phonemes into recognizable words, orally segment single syllable spoken words into '
                        'their phonemes, manipulate individual phonemes to create new words through addition, '
                        'deletion and substitution, and identify the number of syllables in a given word.'
                    ),
                },
                {
                    'id': 'la_lri_3',
                    'text': (
                        'Demonstrate knowledge of the Alphabetic Principle, the ability to associate sounds with '
                        'letters and use these sounds to form words- Generate the sounds of all the letters and '
                        'letter patterns, including consonant blends and long- and short-vowel patterns and blend '
                        'those sounds into recognizable words, use decoding skills to accurately and fluently read '
                        'and spell phonetically regular words, Read common, irregular sight words accurately and '
                        'fluently. (1 st Grade Sight Word List), read compound words and contractions, read '
                        'inflectional forms (e.g., -s, -ed -ing) and root words (e.g. look, looked, looking), and '
                        'reread and self-correct word recognition errors.'
                    ),
                },
                {
                    'id': 'la_lri_4',
                    'text': (
                        'Demonstrate FLUENCY, the ability to read grade level text orally with accuracy, appropriate '
                        'rate and expression- recognize high frequency and familiar words in isolation and in '
                        'context, adjust reading rate based on purpose, text difficulty, form and style, and '
                        'recognize and self-correct errors.'
                    ),
                },
                {
                    'id': 'la_lri_5',
                    'text': (
                        'Demonstrate a rich listening and speaking VOCABULARY, the ability to understand (receptive) '
                        'and use (expressive) words to acquire and convey meaning-Answer comprehension questions '
                        'orally and in writing based on material read independently, understand, learn and use new '
                        'vocabulary from a variety of content areas, and use context to understand word and sentence '
                        'meanings.'
                    ),
                },
            ],
        },
        {
            'title': 'Comprehension and Reading',
            'standards': [
                {
                    'id': 'la_cr_1',
                    'text': (
                        'Identify, analyze and apply knowledge of the elements of a variety of informational texts '
                        'to demonstrate an understanding of the information presented-Identify the difference '
                        'between facts and opinions, identify essential information such as facts, main idea and '
                        'supporting information from illustrations and text, and make predictions about what '
                        'happens next in a story or process and justify.'
                    ),
                },
            ],
        },
        {
            'title': 'READING, ANALYZING AND INTERPRETING LITERATURE',
            'standards': [
                {
                    'id': 'la_rail_1',
                    'text': (
                        'Identify, analyze and apply knowledge of the elements of a variety of fiction and literary '
                        'texts to demonstrate an understanding of a literary selection – Retell the main events of '
                        'a story including characters and setting as well as beginning, middle and end, identify '
                        'the main topic or essential message of a literary selection, make predictions about what '
                        'happens next in a story or process and justify, and connect the reading to self and the '
                        'world and other literary piece.'
                    ),
                },
            ],
        },
        {
            'title': 'TYPES OF WRITING',
            'standards': [
                {
                    'id': 'la_tw_1',
                    'text': (
                        'Narrative: Develop and demonstrate the writing of narrative pieces that include a main '
                        'idea based on read, imagined or personal events, characters and a sequence of events. '
                        'Informational: Develop and demonstrate the writing of informational pieces that provides '
                        'information related real -world tasks. Persuasive: Develop and demonstrate persuasive '
                        'writing that is used for the purpose of influencing the reader.'
                    ),
                },
            ],
        },
        {
            'title': 'QUALITY OF WRITING',
            'standards': [
                {
                    'id': 'la_qw_1',
                    'text': (
                        'Progress through the stages of the writing process: prewriting, drafting, revising, editing '
                        'and publishing- With assistance, write for different purposes and to a specific audience or '
                        'person , brainstorm ideas, organize and select a topic for writing, maintain a focus on a '
                        'single idea while including supporting details, organize details into a logical sequence '
                        'that has a beginning, middle and end , revise writing to add or eliminate details and '
                        'correct ideas so a logical order is present, spell common, frequently used words correctly.'
                    ),
                },
            ],
        },
        {
            'title': 'SPEAKING AND LISTENING',
            'standards': [
                {
                    'id': 'la_sl_1',
                    'text': (
                        'Apply listening and speaking strategies effectively, use electronic media for learning '
                        'purposes, such as generating a journal or story,-Listen attentively and follow directions '
                        'to perform a task (e.g., multi-step oral directions, solving problems and following rules), '
                        'and ask questions to clarify information. Use correct vocabulary and word usage when '
                        'speaking, use appropriate volume while initiating answers and conversation, and use '
                        'appropriate pace in sentences, rhymes, poetry and questions.'
                    ),
                },
            ],
        },
    ],
}

GRADE1_MATH = {
    'title': 'Math',
    'sections': [
        {
            'title': 'NUMBERS, NUMBER SYSTEMS AND NUMBER RELATIONSHIPS',
            'standards': [
                {
                    'id': 'math_nnsr_1',
                    'text': (
                        'Numbers, Numbers Systems and Number Relationships: Count using whole numbers to 100 by '
                        "1's, 2's, 5's, 10's and 25's, use whole numbers and fractions (halves, thirds and "
                        'fourths) to represent quantities, represent equivalent forms of the same number through '
                        'the use of concrete objects, drawings, word names and symbols to 100, Count, compare and '
                        'make change up to one dollar using a collection of coins (pennies, nickels, dimes and '
                        'quarters) Apply number patterns (even and odd) and compare numbers on the hundred chart.'
                    ),
                },
            ],
        },
        {
            'title': 'COMPUTATION AND ESTIMATION',
            'standards': [
                {
                    'id': 'math_ce_1',
                    'text': (
                        'Solve addition and subtraction in everyday situations using concrete objects with one and '
                        'two digit numbers (no regrouping). Determine the sum of the same three one-digit numbers '
                        '(e.g., 5+5+5). Make estimates of objects in a set up to and including 100 using groups '
                        'of ten as a reference and verify estimate.'
                    ),
                },
            ],
        },
        {
            'title': 'MEASUREMENT AND ESTIMATION',
            'standards': [
                {
                    'id': 'math_me_1',
                    'text': (
                        'Compare two objects using direct comparison, estimate and determine the length and height '
                        'of objects using nonstandard units, tell time (analog and digital) to the hour and half '
                        'hour, demonstrate that a single object has different attributes that can be measured in '
                        'different ways (e.g., length, mass/weight, time, area, temperature, capacity, perimeter.'
                    ),
                },
            ],
        },
        {
            'title': 'MATHEMATICAL REASONING AND CONNECTIONS MATHEMATICAL PROBLEM SOLVING AND COMMUNICATION',
            'standards': [
                {
                    'id': 'math_mrc_1',
                    'text': (
                        'Make, check and verify predictions about the quantity, size and shape of objects and '
                        'groups of objects, use measurements in everyday situations, use appropriate problem-solving '
                        'strategies (e.g., make a model, draw a picture, guess and check, working backwards), '
                        'determine when sufficient information is present to solve a problem and explain how to solve '
                        'a problem'
                    ),
                },
            ],
        },
        {
            'title': 'TRIGONOMETRY and CALCULUS',
            'standards': [
                {
                    'id': 'math_tc_1',
                    'text': (
                        'Identify and give examples of corner angles, identify a triangle that has one corner. '
                        'Recognize that a line drawn from one vertex to the opposite vertex in a rectangle or square '
                        'divides it into two triangles, each with a corner angle. Describe how you can get closer '
                        'and closer to a point or line and not reach or cross it.'
                    ),
                },
            ],
        },
        {
            'title': 'ALGEBRA AND FUNCTIONS',
            'standards': [
                {
                    'id': 'math_af_1',
                    'text': (
                        'Recognize and extend patterns based on shape, size, color, sound or number and identify '
                        'the rule for a repeating pattern that could be extended infinitely, find a missing addend '
                        'that makes a number sentence true and explain how solutions to equations or missing '
                        'addends are determined. Identify the missing symbol (+,-, =) that makes a number sentence '
                        'true'
                    ),
                },
            ],
        },
        {
            'title': 'STATISTICS AND DATA ANALYSIS PROBABILITY AND PREDICTIONS',
            'standards': [
                {
                    'id': 'math_sdap_1',
                    'text': (
                        'Formulate and answer questions that can be proven with data (bar graphs, pictographs and/or '
                        'tally charts), collect, organize and display relevant data to answer questions, discuss '
                        'whether a prediction or statement is likely or unlikely based on a comparison to the data, '
                        'state and explain the likelihood of an event using the terms certain, likely, unlikely or '
                        'impossible'
                    ),
                },
            ],
        },
        {
            'title': 'GEOMETRY',
            'standards': [
                {
                    'id': 'math_g_1',
                    'text': (
                        'Name and label geometric shapes in two and three dimensions (e.g., circle, square, triangle, '
                        'rectangle, sphere, cube, pyramid and prism), identify and draw lines of symmetry in '
                        'geometric figures and Identify lines of symmetry in nature, show relationships between and '
                        'among figures using reflections and predict how shapes can be changed by combining or '
                        'dividing them.'
                    ),
                },
            ],
        },
    ],
}

VALID_MARKS = ('M', 'W', 'NA', 'UA')

SUBJECT_CATALOGS = {
    'language_arts': GRADE1_LANGUAGE_ARTS,
    'math': GRADE1_MATH,
}

_STANDARD_SUBJECT = {}
for _subject_key, _subject in SUBJECT_CATALOGS.items():
    for _section in _subject['sections']:
        for _std in _section['standards']:
            _STANDARD_SUBJECT[_std['id']] = _subject_key


def subject_for_standard(standard_id):
    return _STANDARD_SUBJECT.get(standard_id)


def flat_standards(subject_key):
    subject = SUBJECT_CATALOGS.get(subject_key)
    if not subject:
        return []
    out = []
    for section in subject['sections']:
        for std in section['standards']:
            out.append({
                'id': std['id'],
                'section': section.get('title') or '',
                'text': std['text'],
            })
    return out


def _normalize_marks(raw_marks):
    if not raw_marks or not isinstance(raw_marks, dict):
        return {}
    out = {}
    for std_id, quarter_map in raw_marks.items():
        if not isinstance(quarter_map, dict):
            continue
        out[str(std_id)] = {
            q: (quarter_map.get(q) or quarter_map.get(q.lower()) or '')
            for q in QUARTER_COLUMNS
        }
    return out


def get_marks_for_student(student_id, school_year_id, subject_key=None):
    try:
        from models import Grade1StandardMark
    except Exception:
        return {}
    try:
        rows = Grade1StandardMark.query.filter_by(
            student_id=student_id,
            school_year_id=school_year_id,
        ).all()
    except Exception:
        return {}

    out = {}
    for row in rows:
        sid = row.standard_id
        if subject_key and subject_for_standard(sid) != subject_key:
            continue
        out.setdefault(sid, {})[row.quarter] = row.mark
    return out


def get_marks_for_students(student_ids, school_year_id, subject_key=None):
    student_ids = [int(s) for s in (student_ids or []) if s is not None]
    if not student_ids:
        return {}
    try:
        from models import Grade1StandardMark
    except Exception:
        return {}
    try:
        rows = Grade1StandardMark.query.filter(
            Grade1StandardMark.student_id.in_(student_ids),
            Grade1StandardMark.school_year_id == school_year_id,
        ).all()
    except Exception:
        return {}

    out = {sid: {} for sid in student_ids}
    for row in rows:
        if subject_key and subject_for_standard(row.standard_id) != subject_key:
            continue
        student_marks = out.setdefault(row.student_id, {})
        student_marks.setdefault(row.standard_id, {})[row.quarter] = row.mark
    return out


def upsert_mark(student_id, standard_id, school_year_id, quarter, mark, user_id=None):
    try:
        from extensions import db
        from models import Grade1StandardMark
    except Exception:
        return False
    if quarter not in QUARTER_COLUMNS:
        return False
    if not subject_for_standard(standard_id):
        return False

    row = Grade1StandardMark.query.filter_by(
        student_id=student_id,
        standard_id=standard_id,
        school_year_id=school_year_id,
        quarter=quarter,
    ).first()

    clean_mark = (mark or '').strip()
    if clean_mark and clean_mark not in VALID_MARKS:
        return False

    if not clean_mark:
        if row:
            db.session.delete(row)
            return True
        return False

    if row:
        if row.mark == clean_mark and row.updated_by == user_id:
            return False
        row.mark = clean_mark
        row.updated_by = user_id
        return True

    db.session.add(Grade1StandardMark(
        student_id=student_id,
        standard_id=standard_id,
        school_year_id=school_year_id,
        quarter=quarter,
        mark=clean_mark,
        updated_by=user_id,
    ))
    return True


def class_completeness(student_ids, school_year_id, subject_key):
    standards = flat_standards(subject_key)
    standards_count = len(standards)
    students_count = len(student_ids or [])
    per_quarter_total = students_count * standards_count
    overall_total = per_quarter_total * len(QUARTER_COLUMNS)

    quarters = {q: {'filled': 0, 'total': per_quarter_total, 'percent': 0} for q in QUARTER_COLUMNS}
    last_updated = None

    if not student_ids or standards_count == 0:
        return {
            'total_cells_per_quarter': per_quarter_total,
            'standards_count': standards_count,
            'students_count': students_count,
            'quarters': quarters,
            'overall': {'filled': 0, 'total': overall_total, 'percent': 0},
            'last_updated': None,
        }

    try:
        from models import Grade1StandardMark
        rows = Grade1StandardMark.query.filter(
            Grade1StandardMark.student_id.in_([int(s) for s in student_ids]),
            Grade1StandardMark.school_year_id == school_year_id,
        ).all()
    except Exception:
        rows = []

    standard_ids_in_subject = {s['id'] for s in standards}
    total_filled = 0
    for r in rows:
        if r.standard_id not in standard_ids_in_subject:
            continue
        if r.quarter in quarters:
            quarters[r.quarter]['filled'] += 1
            total_filled += 1
        if r.updated_at and (last_updated is None or r.updated_at > last_updated):
            last_updated = r.updated_at

    for stats in quarters.values():
        stats['percent'] = int(round(100 * stats['filled'] / stats['total'])) if stats['total'] else 0

    overall_percent = int(round(100 * total_filled / overall_total)) if overall_total else 0
    return {
        'total_cells_per_quarter': per_quarter_total,
        'standards_count': standards_count,
        'students_count': students_count,
        'quarters': quarters,
        'overall': {'filled': total_filled, 'total': overall_total, 'percent': overall_percent},
        'last_updated': last_updated,
    }


def section_completeness(student_ids, school_year_id, subject_key, quarter):
    subject = SUBJECT_CATALOGS.get(subject_key)
    if not subject or not student_ids:
        return {}

    sections = []
    section_for_std = {}
    for section in subject['sections']:
        title = section.get('title') or ''
        std_ids = [s['id'] for s in section['standards']]
        sections.append({'title': title, 'std_ids': std_ids})
        for sid in std_ids:
            section_for_std[sid] = title

    try:
        from models import Grade1StandardMark
        rows = Grade1StandardMark.query.filter(
            Grade1StandardMark.student_id.in_([int(s) for s in student_ids]),
            Grade1StandardMark.school_year_id == school_year_id,
            Grade1StandardMark.quarter == quarter,
        ).all()
    except Exception:
        rows = []

    students_count = len(student_ids)
    result = {}
    for sect in sections:
        total = students_count * len(sect['std_ids'])
        result[sect['title']] = {'filled': 0, 'total': total, 'percent': 0}

    for r in rows:
        sect_title = section_for_std.get(r.standard_id)
        if sect_title is not None:
            result[sect_title]['filled'] += 1

    for stats in result.values():
        stats['percent'] = int(round(100 * stats['filled'] / stats['total'])) if stats['total'] else 0
    return result


def copy_marks_from_previous_quarter(student_ids, school_year_id, subject_key, target_quarter, user_id=None):
    if target_quarter not in QUARTER_COLUMNS:
        return 0
    idx = QUARTER_COLUMNS.index(target_quarter)
    if idx == 0:
        return 0
    prev_q = QUARTER_COLUMNS[idx - 1]

    existing = get_marks_for_students(student_ids, school_year_id, subject_key=subject_key)
    copied = 0
    for sid in student_ids:
        per_std = existing.get(sid, {})
        for std_id, per_q in per_std.items():
            prev_mark = per_q.get(prev_q)
            if not prev_mark:
                continue
            if per_q.get(target_quarter):
                continue
            if upsert_mark(sid, std_id, school_year_id, target_quarter, prev_mark, user_id=user_id):
                copied += 1
    return copied


def grade1_standards_context(report_card_data=None, student_id=None, school_year_id=None):
    saved = {}
    if isinstance(report_card_data, dict):
        saved = report_card_data.get('standards_marks') or {}

    la_marks = _normalize_marks(saved.get('language_arts') if isinstance(saved, dict) else {})
    math_marks = _normalize_marks(saved.get('math') if isinstance(saved, dict) else {})

    if student_id and school_year_id:
        live = get_marks_for_student(student_id, school_year_id)
        for std_id, per_q in live.items():
            subject = subject_for_standard(std_id)
            target = la_marks if subject == 'language_arts' else math_marks
            existing = target.get(std_id, {q: '' for q in QUARTER_COLUMNS})
            for q in QUARTER_COLUMNS:
                v = per_q.get(q)
                if v:
                    existing[q] = v
            target[std_id] = existing

    return {
        'grade1_language_arts': GRADE1_LANGUAGE_ARTS,
        'grade1_math': GRADE1_MATH,
        'grade1_language_arts_marks': la_marks,
        'grade1_math_marks': math_marks,
        'grade1_quarter_columns': QUARTER_COLUMNS,
    }
